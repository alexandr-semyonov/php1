1. var_dump (1 == 1.0);
Ќестрогое сравнение без учета типов. „исло с плавающей точкой будет преобразовано в целое.

2. var_dump (1 === 1.0);
—трогое сравнение с учетом типов. ѕреобразовани€ типов не происходит, поэтому целое число 1 не равно числу с плавающей точкой 1.0.

3. var_dump ('02' == 2);
Ќестрогое сравнение без учета типов. —трока будет преобразована в число 2 и сравниватьс€ будут числа.

4. var_dump ('02' === 2);
—трогое сравнение с учетом типов. ѕреобразовани€ типов не происходит, поэтому строка 02 не равна целому числу 2

5. var_dump ('02' == '2');
Ќестрогое сравнение без учета типов.  ажда€ строка будет преобразована в число и сравниватьс€ будут как числа.